spring:
    application:
        name: ApplicationSpringThApplication
    datasource:
        url: jdbc:mysql://localhost3306/app_store
        username: root
        password: root
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa: 
        hibernate.ddl-auto: update
        show-sql: true
        properties:
            hibernate:
            dialect: org.hibernate.dialect.MySQLDialect